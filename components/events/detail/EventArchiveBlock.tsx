export default function EventArchiveBlock() {
  return (
    <section className="mt-8 rounded-[2rem] border border-amber-200 bg-amber-50 p-6 shadow-sm">
      <h2 className="text-2xl font-semibold text-amber-900">This event has ended</h2>
      <p className="mt-3 text-base leading-7 text-amber-800">
        JaipurCircle keeps event pages live even after they close, so the city builds a permanent event memory.
        Explore similar upcoming events below.
      </p>
    </section>
  );
}
